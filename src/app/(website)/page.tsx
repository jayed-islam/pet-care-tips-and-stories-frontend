import HomeView from "@/sections/home/view/home-view";
import React from "react";

export const metadata = {
  title: "Eyebook | Home",
};

const Home = () => {
  return <HomeView />;
};

export default Home;

import HomeView from "@/sections/home/view/home-new-view";
import React from "react";

export const metadata = {
  title: "Eyebook | Home",
};

const Home = () => {
  return <HomeView />;
};

export default Home;

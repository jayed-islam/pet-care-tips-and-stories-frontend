import PageList from "@/sections/dashboard/view/page-list-view";
import React from "react";

export const metadata = {
  title: "Pages",
};

const Page = () => {
  return <PageList />;
};

export default Page;

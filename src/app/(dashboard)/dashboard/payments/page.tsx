import PaymentListView from "@/sections/dashboard/view/payment-lit-view";
import React from "react";

export const metadata = {
  title: "Payments",
};

const Page = () => {
  return <PaymentListView />;
};

export default Page;

import UserListView from "@/sections/dashboard/view/users-list-view";
import React from "react";

export const metadata = {
  title: "Users",
};

const Page = () => {
  return <UserListView />;
};

export default Page;

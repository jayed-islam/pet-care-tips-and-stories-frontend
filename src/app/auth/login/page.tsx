import LoginPageView from "@/sections/auth/user-login-view";
import React from "react";

export const metadata = {
  title: "Login",
};

const LoginPage = () => {
  return <LoginPageView />;
};

export default LoginPage;
